/*
=======================================================
ИМПОРТЫ ПЛАГИНОВ 
=======================================================
*/

import clean from "gulp-clean";

/*
=======================================================
ВЫПОЛНЕНИЕ ЗАДАЧИ
=======================================================
*/
export const cleanDir = () => {
    return app.gulp
        .src("dist", { read: false, allowEmpty: true })
        .pipe(clean()); // Очистка директории при обновлении сервера
};
